---
title: "Stamp `processed_at` / `processed_by`"
description: "Stamp `processed_at` / `processed_by`."
api: "POST /api/v1/ingest/events/:id/processed"
---

## Purpose

Stamp `processed_at` / `processed_by`. See the live route handler at `packages/ctrl/src/api/routes/ingest.ts:110` for the authoritative request/response contract.

<Note>
This endpoint is part of the Alfred Black API. The OpenAPI spec for this endpoint is being authored — Mintlify will render parameters, request examples, and response examples here once the spec lands. Until then, see the source citation above for the authoritative shape.
</Note>

## Try it

<CodeGroup>

```bash cURL
curl -X POST https://alfred.black/api/v1/ingest/events/:id/processed \
 -H "Authorization: Bearer ${AAS_API_KEY}"
```

```javascript JavaScript
const r = await fetch('https://alfred.black/api/v1/ingest/events/:id/processed', {
 method: 'POST',
 headers: { 'Authorization': `Bearer ${process.env.AAS_API_KEY}` },
});
```

</CodeGroup>
