---
title: "Insert a `stream_event` row"
description: "Insert a `stream_event` row. Body: `{stream, channel?, external_id?, kind?, payload}`."
api: "POST /api/v1/ingest/events"
---

## Purpose

Insert a `stream_event` row. Body: `{stream, channel?, external_id?, kind?, payload}`. See the live route handler at `packages/ctrl/src/api/routes/ingest.ts:34` for the authoritative request/response contract.

<Note>
This endpoint is part of the Alfred Black API. The OpenAPI spec for this endpoint is being authored — Mintlify will render parameters, request examples, and response examples here once the spec lands. Until then, see the source citation above for the authoritative shape.
</Note>

## Try it

<CodeGroup>

```bash cURL
curl -X POST https://alfred.black/api/v1/ingest/events \
 -H "Authorization: Bearer ${AAS_API_KEY}"
```

```javascript JavaScript
const r = await fetch('https://alfred.black/api/v1/ingest/events', {
 method: 'POST',
 headers: { 'Authorization': `Bearer ${process.env.AAS_API_KEY}` },
});
```

</CodeGroup>
