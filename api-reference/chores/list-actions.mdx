---
title: "Catalogue of available chore-action primitives"
description: "Catalogue of available chore-action primitives."
api: "GET /api/v1/chore-actions"
---

## Purpose

Catalogue of available chore-action primitives. See the live route handler at `packages/ctrl/src/api/routes/chores.ts:549` for the authoritative request/response contract.

<Note>
This endpoint is part of the Alfred Black API. The OpenAPI spec for this endpoint is being authored — Mintlify will render parameters, request examples, and response examples here once the spec lands. Until then, see the source citation above for the authoritative shape.
</Note>

## Try it

<CodeGroup>

```bash cURL
curl -X GET https://alfred.black/api/v1/chore-actions \
 -H "Authorization: Bearer ${AAS_API_KEY}"
```

```javascript JavaScript
const r = await fetch('https://alfred.black/api/v1/chore-actions', {
 method: 'GET',
 headers: { 'Authorization': `Bearer ${process.env.AAS_API_KEY}` },
});
```

</CodeGroup>
