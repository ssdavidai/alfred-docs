---
title: "List Observations"
description: "List with filters (`kind`, `subject`, `since`, `processed_at` null/not-null)."
api: "GET /api/v1/state/observations"
---

## Purpose

List with filters (`kind`, `subject`, `since`, `processed_at` null/not-null). See the live route handler at `packages/ctrl/src/api/routes/state.ts:275` for the authoritative request/response contract.

<Note>
This endpoint is part of the Alfred Black API. The OpenAPI spec for this endpoint is being authored — Mintlify will render parameters, request examples, and response examples here once the spec lands. Until then, see the source citation above for the authoritative shape.
</Note>

## Try it

<CodeGroup>

```bash cURL
curl -X GET https://alfred.black/api/v1/state/observations \
 -H "Authorization: Bearer ${AAS_API_KEY}"
```

```javascript JavaScript
const r = await fetch('https://alfred.black/api/v1/state/observations', {
 method: 'GET',
 headers: { 'Authorization': `Bearer ${process.env.AAS_API_KEY}` },
});
```

</CodeGroup>
