---
title: "Upsert a vector"
description: "Upsert a vector. Body: `{ref, ref_kind, model?, chunk_index?, text_preview?, vector}`."
api: "POST /api/v1/state/embeddings"
---

## Purpose

Upsert a vector. Body: `{ref, ref_kind, model?, chunk_index?, text_preview?, vector}`. See the live route handler at `packages/ctrl/src/api/routes/state.ts:589` for the authoritative request/response contract.

<Note>
This endpoint is part of the Alfred Black API. The OpenAPI spec for this endpoint is being authored — Mintlify will render parameters, request examples, and response examples here once the spec lands. Until then, see the source citation above for the authoritative shape.
</Note>

## Try it

<CodeGroup>

```bash cURL
curl -X POST https://alfred.black/api/v1/state/embeddings \
 -H "Authorization: Bearer ${AAS_API_KEY}"
```

```javascript JavaScript
const r = await fetch('https://alfred.black/api/v1/state/embeddings', {
 method: 'POST',
 headers: { 'Authorization': `Bearer ${process.env.AAS_API_KEY}` },
});
```

</CodeGroup>
