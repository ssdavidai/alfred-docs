---
title: "Update `status` / `confidence` / `processed_at`"
description: "Update `status` / `confidence` / `processed_at`."
api: "PATCH /api/v1/state/observations/:id"
---

## Purpose

Update `status` / `confidence` / `processed_at`. See the live route handler at `packages/ctrl/src/api/routes/state.ts:303` for the authoritative request/response contract.

<Note>
This endpoint is part of the Alfred Black API. The OpenAPI spec for this endpoint is being authored — Mintlify will render parameters, request examples, and response examples here once the spec lands. Until then, see the source citation above for the authoritative shape.
</Note>

## Try it

<CodeGroup>

```bash cURL
curl -X PATCH https://alfred.black/api/v1/state/observations/:id \
 -H "Authorization: Bearer ${AAS_API_KEY}"
```

```javascript JavaScript
const r = await fetch('https://alfred.black/api/v1/state/observations/:id', {
 method: 'PATCH',
 headers: { 'Authorization': `Bearer ${process.env.AAS_API_KEY}` },
});
```

</CodeGroup>
