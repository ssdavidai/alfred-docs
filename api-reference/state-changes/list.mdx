---
title: "List with filters (`target_path`, `field`, `since`)"
description: "List with filters (`target_path`, `field`, `since`)."
api: "GET /api/v1/state-changes"
---

## Purpose

List with filters (`target_path`, `field`, `since`). See the live route handler at `packages/ctrl/src/api/routes/stateChanges.ts:633` for the authoritative request/response contract.

<Note>
This endpoint is part of the Alfred Black API. The OpenAPI spec for this endpoint is being authored — Mintlify will render parameters, request examples, and response examples here once the spec lands. Until then, see the source citation above for the authoritative shape.
</Note>

## Try it

<CodeGroup>

```bash cURL
curl -X GET https://alfred.black/api/v1/state-changes \
 -H "Authorization: Bearer ${AAS_API_KEY}"
```

```javascript JavaScript
const r = await fetch('https://alfred.black/api/v1/state-changes', {
 method: 'GET',
 headers: { 'Authorization': `Bearer ${process.env.AAS_API_KEY}` },
});
```

</CodeGroup>
