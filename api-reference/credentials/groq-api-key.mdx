---
title: "Read-back for the voice container"
description: "Read-back for the voice container."
api: "GET /api/v1/credentials/groq-api-key"
---

## Purpose

Read-back for the voice container. See the live route handler at `packages/ctrl/src/api/routes/channels_omi.ts:509` for the authoritative request/response contract.

<Note>
This endpoint is part of the Alfred Black API. The OpenAPI spec for this endpoint is being authored — Mintlify will render parameters, request examples, and response examples here once the spec lands. Until then, see the source citation above for the authoritative shape.
</Note>

## Try it

<CodeGroup>

```bash cURL
curl -X GET https://alfred.black/api/v1/credentials/groq-api-key \
 -H "Authorization: Bearer ${AAS_API_KEY}"
```

```javascript JavaScript
const r = await fetch('https://alfred.black/api/v1/credentials/groq-api-key', {
 method: 'GET',
 headers: { 'Authorization': `Bearer ${process.env.AAS_API_KEY}` },
});
```

</CodeGroup>
