---
title: "Undo by replaying the inverse recipe"
description: "Undo by replaying the inverse recipe."
api: "POST /api/v1/steward/undo/:action_id"
---

## Purpose

Undo by replaying the inverse recipe. See the live route handler at `packages/ctrl/src/api/routes/steward.ts:461` for the authoritative request/response contract.

<Note>
This endpoint is part of the Alfred Black API. The OpenAPI spec for this endpoint is being authored — Mintlify will render parameters, request examples, and response examples here once the spec lands. Until then, see the source citation above for the authoritative shape.
</Note>

## Try it

<CodeGroup>

```bash cURL
curl -X POST https://alfred.black/api/v1/steward/undo/:action_id \
 -H "Authorization: Bearer ${AAS_API_KEY}"
```

```javascript JavaScript
const r = await fetch('https://alfred.black/api/v1/steward/undo/:action_id', {
 method: 'POST',
 headers: { 'Authorization': `Bearer ${process.env.AAS_API_KEY}` },
});
```

</CodeGroup>
