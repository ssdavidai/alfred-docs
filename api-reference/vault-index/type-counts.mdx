---
title: "Per-type counts (matter: 47, task: 312, …)"
description: "Per-type counts (matter: 47, task: 312, …)."
api: "GET /api/v1/vault-index/type-counts"
---

## Purpose

Per-type counts (matter: 47, task: 312, …). See the live route handler at `packages/ctrl/src/api/routes/vaultIndex.ts:52` for the authoritative request/response contract.

<Note>
This endpoint is part of the Alfred Black API. The OpenAPI spec for this endpoint is being authored — Mintlify will render parameters, request examples, and response examples here once the spec lands. Until then, see the source citation above for the authoritative shape.
</Note>

## Try it

<CodeGroup>

```bash cURL
curl -X GET https://alfred.black/api/v1/vault-index/type-counts \
 -H "Authorization: Bearer ${AAS_API_KEY}"
```

```javascript JavaScript
const r = await fetch('https://alfred.black/api/v1/vault-index/type-counts', {
 method: 'GET',
 headers: { 'Authorization': `Bearer ${process.env.AAS_API_KEY}` },
});
```

</CodeGroup>
