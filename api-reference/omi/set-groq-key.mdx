---
title: "Set the Groq key"
description: "Set the Groq key."
api: "PUT /api/v1/channels/omi/groq-key"
---

## Purpose

Set the Groq key. See the live route handler at `packages/ctrl/src/api/routes/channels_omi.ts:410` for the authoritative request/response contract.

<Note>
This endpoint is part of the Alfred Black API. The OpenAPI spec for this endpoint is being authored — Mintlify will render parameters, request examples, and response examples here once the spec lands. Until then, see the source citation above for the authoritative shape.
</Note>

## Try it

<CodeGroup>

```bash cURL
curl -X PUT https://alfred.black/api/v1/channels/omi/groq-key \
 -H "Authorization: Bearer ${AAS_API_KEY}"
```

```javascript JavaScript
const r = await fetch('https://alfred.black/api/v1/channels/omi/groq-key', {
 method: 'PUT',
 headers: { 'Authorization': `Bearer ${process.env.AAS_API_KEY}` },
});
```

</CodeGroup>
