---
title: "Tweak the compose memory caps"
description: "Tweak the compose memory caps."
api: "POST /api/v1/admin/compose/fix-memory"
---

## Purpose

Tweak the compose memory caps. See the live route handler at `packages/ctrl/src/api/routes/admin.ts:954` for the authoritative request/response contract.

<Note>
This endpoint is part of the Alfred Black API. The OpenAPI spec for this endpoint is being authored — Mintlify will render parameters, request examples, and response examples here once the spec lands. Until then, see the source citation above for the authoritative shape.
</Note>

## Try it

<CodeGroup>

```bash cURL
curl -X POST https://alfred.black/api/v1/admin/compose/fix-memory \
 -H "Authorization: Bearer ${AAS_API_KEY}"
```

```javascript JavaScript
const r = await fetch('https://alfred.black/api/v1/admin/compose/fix-memory', {
 method: 'POST',
 headers: { 'Authorization': `Bearer ${process.env.AAS_API_KEY}` },
});
```

</CodeGroup>
