---
title: "List In Flight"
description: "Decisions currently awaiting an agent outcome (`state=executing`)."
api: "GET /api/v1/decisions/in-flight"
---

## Purpose

Decisions currently awaiting an agent outcome (`state=executing`). See the live route handler at `packages/ctrl/src/api/routes/decisions.ts:764` for the authoritative request/response contract.

<Note>
This endpoint is part of the Alfred Black API. The OpenAPI spec for this endpoint is being authored — Mintlify will render parameters, request examples, and response examples here once the spec lands. Until then, see the source citation above for the authoritative shape.
</Note>

## Try it

<CodeGroup>

```bash cURL
curl -X GET https://alfred.black/api/v1/decisions/in-flight \
 -H "Authorization: Bearer ${AAS_API_KEY}"
```

```javascript JavaScript
const r = await fetch('https://alfred.black/api/v1/decisions/in-flight', {
 method: 'GET',
 headers: { 'Authorization': `Bearer ${process.env.AAS_API_KEY}` },
});
```

</CodeGroup>
