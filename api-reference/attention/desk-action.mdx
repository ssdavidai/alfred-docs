---
title: "Generic manual desk-action audit"
description: "Generic manual desk-action audit."
api: "POST /api/v1/admin/desk-action"
---

## Purpose

Generic manual desk-action audit. See the live route handler at `packages/ctrl/src/api/routes/attention.ts:688` for the authoritative request/response contract.

<Note>
This endpoint is part of the Alfred Black API. The OpenAPI spec for this endpoint is being authored — Mintlify will render parameters, request examples, and response examples here once the spec lands. Until then, see the source citation above for the authoritative shape.
</Note>

## Try it

<CodeGroup>

```bash cURL
curl -X POST https://alfred.black/api/v1/admin/desk-action \
 -H "Authorization: Bearer ${AAS_API_KEY}"
```

```javascript JavaScript
const r = await fetch('https://alfred.black/api/v1/admin/desk-action', {
 method: 'POST',
 headers: { 'Authorization': `Bearer ${process.env.AAS_API_KEY}` },
});
```

</CodeGroup>
