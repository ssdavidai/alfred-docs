---
title: "Update Transactions"
description: "PATCH /api/v1/sure/transactions/:id"
api: "PATCH /api/v1/sure/transactions/:id"
---

## Purpose

`PATCH /api/v1/sure/transactions/:id` — see live source for the authoritative contract. See the live route handler at `packages/ctrl/src/api/routes/sure.ts:573` for the authoritative request/response contract.

<Note>
This endpoint is part of the Alfred Black API. The OpenAPI spec for this endpoint is being authored — Mintlify will render parameters, request examples, and response examples here once the spec lands. Until then, see the source citation above for the authoritative shape.
</Note>

## Try it

<CodeGroup>

```bash cURL
curl -X PATCH https://alfred.black/api/v1/sure/transactions/:id \
 -H "Authorization: Bearer ${AAS_API_KEY}"
```

```javascript JavaScript
const r = await fetch('https://alfred.black/api/v1/sure/transactions/:id', {
 method: 'PATCH',
 headers: { 'Authorization': `Bearer ${process.env.AAS_API_KEY}` },
});
```

</CodeGroup>
