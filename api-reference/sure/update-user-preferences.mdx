---
title: "Edit prefs"
description: "Edit prefs."
api: "PATCH /api/v1/sure/user/preferences"
---

## Purpose

Edit prefs. See the live route handler at `packages/ctrl/src/api/routes/sure.ts:860` for the authoritative request/response contract.

<Note>
This endpoint is part of the Alfred Black API. The OpenAPI spec for this endpoint is being authored — Mintlify will render parameters, request examples, and response examples here once the spec lands. Until then, see the source citation above for the authoritative shape.
</Note>

## Try it

<CodeGroup>

```bash cURL
curl -X PATCH https://alfred.black/api/v1/sure/user/preferences \
 -H "Authorization: Bearer ${AAS_API_KEY}"
```

```javascript JavaScript
const r = await fetch('https://alfred.black/api/v1/sure/user/preferences', {
 method: 'PATCH',
 headers: { 'Authorization': `Bearer ${process.env.AAS_API_KEY}` },
});
```

</CodeGroup>
