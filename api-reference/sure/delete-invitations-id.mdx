---
title: "Cancel"
description: "Cancel."
api: "DELETE /api/v1/sure/invitations/:id"
---

## Purpose

Cancel. See the live route handler at `packages/ctrl/src/api/routes/sure.ts:829` for the authoritative request/response contract.

<Note>
This endpoint is part of the Alfred Black API. The OpenAPI spec for this endpoint is being authored — Mintlify will render parameters, request examples, and response examples here once the spec lands. Until then, see the source citation above for the authoritative shape.
</Note>

## Try it

<CodeGroup>

```bash cURL
curl -X DELETE https://alfred.black/api/v1/sure/invitations/:id \
 -H "Authorization: Bearer ${AAS_API_KEY}"
```

```javascript JavaScript
const r = await fetch('https://alfred.black/api/v1/sure/invitations/:id', {
 method: 'DELETE',
 headers: { 'Authorization': `Bearer ${process.env.AAS_API_KEY}` },
});
```

</CodeGroup>
