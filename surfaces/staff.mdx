---
title: "Staff"
description: "The small staff of specialists employed on the principal's behalf."
---

<Info>
**Status — marketing surface, placeholder for future copy.** Alfred's landing page promises *"a small staff of specialists, employed on your behalf"* *(packages/web/src/landing-page/LandingPage.tsx:87)*. `/staff` is a placeholder slot in the surface vocabulary where a dedicated roster page will live; this documentation page describes the concept the slot will fill. The staff itself is real today, just distributed across the surfaces listed below.
</Info>

## The metaphor

The Alfred Black design language frames the system as a household: a butler ("Alfred") who employs a small staff of specialists, each tuned to a different kind of work. The principal never sees the staff directly; they see the work arrive, signed by Alfred *(packages/web/src/landing-page/LandingPage.tsx:140-170)*.

## The actual roster

Under the metaphor, every "specialist" is a named agent bound to a Hermes profile. The live tenant catalogue reports these stable agent ids; deployments may change the model names, but the split between profiles is the current runtime shape:

<CardGroup cols={3}>
 <Card title="Alfred" href="/surfaces/channels">
 Agent id **`main`**. The principal's conversational counterpart on every channel — Slack, Telegram, SMS, the web app, and email replies. Hermes profile: **`main`**.
 </Card>
 <Card title="Clerk">
 Agent id **`learn-clerk`**. Event classification, extraction, and reflection — the cheap, high-volume work. Hermes profile: **`workers`**.
 </Card>
 <Card title="Curator">
 Agent id **`vault-curator`**. Processes inbox material into structured vault records. Hermes profile: **`workers`**.
 </Card>
 <Card title="Janitor">
 Agent id **`vault-janitor`**. Fixes structural vault issues and frontmatter problems. Hermes profile: **`workers`**.
 </Card>
 <Card title="Distiller">
 Agent id **`vault-distiller`**. Extracts latent knowledge from vault records. Hermes profile: **`workers`**.
 </Card>
 <Card title="Onboarding">
 Agent id **`onboarding`**. Heavy fact and pattern reasoning during setup. Hermes profile: **`heavy`**.
 </Card>
 <Card title="Chore reasoning">
 Agent id **`chore`**. Heavy-reasoning steps inside recurring workflows. Hermes profile: **`heavy`**.
 </Card>
</CardGroup>

The surveyor is not exposed as a separate staff card; it is a worker-side service that labels records and writes embeddings for semantic vault search.

## Why the principal sees one face

The principal interacts with **Alfred** — singular, signed, in one voice. The staff exists so different kinds of work can ride different cost-and-latency budgets without the principal having to think about which one is on call. Every chore, every onboarding step, every clerk reflection is internally dispatched to the right profile by ctrl-api's `agents` route; the answer comes back to the principal as Alfred's *(packages/ctrl/src/api/routes/agents.ts)*.

## How a specialist gets work

The pipeline that connects inbound signals to staff dispatch is detailed in [Signal pipeline](/architecture/signal-pipeline). Briefly:

1. A stream event lands (email, Composio webhook, OMI audio chunk, calendar push).
2. **Workers / clerk** extract a signal from it; **workers / surveyor** indexes the artifacts.
3. The signal router matches the signal against known instincts; if confidence clears the bar, **workers** dispatch the autonomous action.
4. If it doesn't, **Alfred (main)** surfaces the matter on `/desk` and asks the principal what to do.
5. The principal's click feeds back into reflection — drafted nightly by **workers**, refined by **heavy** when patterns warrant a deeper look.

## Where to manage them

| Surface | What you can do |
|---|---|
| [Settings → Agent Configuration](/surfaces/study) | Swap the model bound to each profile (every agent on that profile moves together) |
| [Tools](/surfaces/tools) | See exactly which Composio actions, MCP servers, and built-in primitives each specialist may reach |
| [Chores](/surfaces/chores) | See which chore the workers run and on what cadence |
| [Decisions](/surfaces/decisions) | Audit the work the staff did on the principal's behalf |

## When `/staff` ships

The dedicated `/staff` page will collapse the surfaces above into one roster — a card per specialist with current model, recent work, cost-per-day, and an "out of office" toggle. Until then, the staff is real but distributed across the surfaces above.

<Note>
The roster lives in **runtime config**, not source. Add a profile by editing `packages/hermes/profiles/<name>/config.yaml`; add an agent and bind it to a profile by editing the agents table in ctrl-api. The Settings page surfaces whatever ctrl-api reports.
</Note>
